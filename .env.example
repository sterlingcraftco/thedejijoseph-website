# Supabase (public, client-side)
VITE_SUPABASE_PROJECT_ID=""
VITE_SUPABASE_PUBLISHABLE_KEY=""
VITE_SUPABASE_URL=""

# Ghost Admin API key — format: {id}:{secret} (server-side, secret)
GHOST_ADMIN_API_KEY="your_ghost_admin_api_key_here"

# Base path the frontend uses for API calls (Caddy proxies /api -> Hono :3000)
VITE_API_BASE_URL="/api"
